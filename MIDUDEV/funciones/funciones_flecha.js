// conocidas en ingles como arrow functions

/*Las funciones flecha son una forma más concisa de crear funciones en JavaScript, 
y se han vuelto muy populares en los últimos años debido a su sintaxis simplificada.*/

//tienen return implñicito
const Sumar = (a, b) => {
return a+b;
}
alert(`la suma de ambos numeros es: ${Sumar(1,2)}`);