
5:10:02 - Apertura del capitulo 4
5:12:52 - Métodos de cadenas
20 métodos de cadenas.
concat()   junta dos o mas cadenas y retorna una nueva.
startWith() -si una cadena comienza com los caracteres de otra  cadena, devuelve true, sino devuelve false.
endWhit() - si la cadena termina con los caracteres de otra cadena, devuelve true, sino devuelve false.
includes() -  si un cadena puede encontrarse dentro de otra cadena, devuelve true, sino devuelve false.
indexOf() - devuelve el indice  del primer caracter de la cadena, si no existe devuelve -1
lasIndexOf() - devuelve el ultimo del primer caracer de la cadena, si no existe devuelve -1

padStart() - [propuesta de estandar] - Rellenar cadena al principio 

5:42:43 - Métodos de arrays
_______________transformadores__________
-pop() elimina el ultimo elemento del arrays y lo devuelve.
-shift()  elimina el primer elemento del arrays y lo devuelve
-reverse() invierte el orden de los elementops del array.
-unshift() iagreg uno o mas elementos al inicio del array, y devuelve la nueva longitud del array
-sort() ordena los elementos de un arreglo (array) localmente y devuelve el arreglo ordenado
-splice() cambia el contenido del array eliminando elementos existentes y/o agregando nuevos elementos.

__________Accesores___________
-join() une todos los elementos de una matriz (u objeto similar ) en cada una de las cadenas y lo devuelve.
-slice() devuelve una parte del array dentro de un array empezando por el inicio hasta el fin(inicio no incluido)ç
- Métodos ya vistos en cadenas: toString(), 
_________de repetición__________
filter(), crea unj un nuevo array con todos los elementos que cumplan una condicion
forEach() ejecuta una funcion indicada una vez  por cada elemento del array

6:08:10 - Objeto Math
-sqrt(), devuelve la raiz cuadrada.
-cbrt(), devuelve la raiz cubica.
-max(), devuelve  el numero mayor.
-min(), devuelve el numero menor de todos.
-random(), devuelve numeros aleatorio entre 0 y 1.
-fround(), devuelve la representacion flotante mas cercana de un numero.
-floor() - devuelve el mayor entero menor o igual a un numero.
-trunc (devuelve la parte entera de un numero x, la eliminacion de digitos fraccionarios)
-
6:21:21 - Historia de Cofla 4 (Crear soluciones)
